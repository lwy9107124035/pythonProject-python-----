计数器：
always @(posedge clk or posedge rst_n)begin
    if(rst_n)begin
        cnt0 <= 0;
    end
    else if(add_cnt0)begin
        if(end_cnt0)
            cnt0 <= 0;
        else
            cnt0 <= cnt0 + 1;
    end
end

assign add_cnt0 =  ;
assign end_cnt0 = add_cnt0 && cnt0 ==  -1;

时序：
always @(posedge clk or posedge rst_n)begin
    if(rst_n==1'b0)begin
    end
    else if()begin
    end
    else if()begin
    end
end

组合：
always @(*)begin
	if()begin
	end
	else if() begin
	end
	else begin
	end
end






